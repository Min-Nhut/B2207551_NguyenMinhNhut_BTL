# Bỏ thư mục node_modules cho cả backend & frontend
/backend/node_modules
/frontend/node_modules

# Bỏ file log & build tạm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Bỏ file môi trường (nếu dùng)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# VSCode folder settings (nếu không muốn share)
.vscode/

# Vite build folder (nếu có)
dist/

# Output build nếu có thư mục riêng
/build

